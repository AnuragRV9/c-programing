#include <iostream>
#include <cmath>
int main()
{
    double a =3.147;
    double b = 4;
    double c;

    // std::cout << "enter a no. : ";
    // std::cin >> a;

    // std::cout << "enter a no. : ";
    // std::cin >> b;

    // c = std::max(a, b);
    //  c = std::min(a, b);
    // c = pow(a, b);
    // c = sqrt(b);
    // c=abs(-3);
    // c=round(a);
    // c = ceil(a);
    // c = floor(a);

    std::cout << c;
}
